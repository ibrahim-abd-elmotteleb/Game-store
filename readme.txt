1. Run back end server by going to the back end folder and running the command 'npm install' then 'npm start'
2. Run front end server by going to the from end folder and running the command 'npm install' then 'npm run dev'
